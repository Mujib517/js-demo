// const arr = [1, 2, 3, 4, 5];

// arr.forEach(elem => console.log(elem));

// function even(elem) {
//     return elem % 2 === 0;
// }

// function odd(elem) {
//     return elem % 2 !== 0;
// }

// // origin: 4, map: 4
// // origin: 4, filter: <=4
// const result = arr.filter(odd);

// const sum = arr.reduce((acc, elem) => acc + elem);

// console.log(sum);

// Javascript
// value types & reference types
// truthy and falsy
// angular properties, emit event, sibling (common parent, service, Redux, subjects)
// Data types
// functions
// parameters
// js modes
// objects
// this keyword
// object inheritance // prototypes
// async js (callbacks, promises, observables, async await)
// es6

// every function returns 
function add(a, b, a) {
    var c = a + b;
    return undefined;
}

class Mobile {

}

function Mobile() {
    this.width = 10;
    this.height = 20;
}

var m = new Mobile();
typeof m;


App
    Home
    About